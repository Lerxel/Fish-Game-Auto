# Fish-Game-Auto
Automatically plays the silly fish game for you!


AUTO CONTROLLER - SETUP INSTRUCTIONS

REQUIRED MODULES:

Install them using this command:

pip install pyautogui keyboard opencv-python numpy pillow

----------------------------------------------------

OPTIONAL (ONLY if you later add tray or image UI):
pip install pystray
